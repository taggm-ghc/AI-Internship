# Secrets — never commit API keys or local credentials
.env
.env.local
*.pem
*-prv
*-prv/
secrets.toml
.streamlit/secrets.toml

# Local run artifacts (e.g. run.sh recording its active host:port)
.faststream-local-url

# LAN-local inference availability log — will contain locally-hosted model
# names once used, which must never enter this portfolio-facing repo (see
# providers.py's AVAILABILITY_LOG_PATH docstring)
.model-availability.json

# Planning
p3m3/
syllabus/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# OS / editor
.DS_Store
.idea/
.vscode/
.cursor/

# Local scratch (not part of the course repo)
ai-eng-demo-1/
